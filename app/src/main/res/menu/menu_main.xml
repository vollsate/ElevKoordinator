<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">

    <item
        android:id="@+id/menu.overflow"
        android:icon="@drawable/ic_action_overflow"
        android:orderInCategory="1"
        android:title="@string/action_settings"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/menu.newTask"
                android:icon="@drawable/ic_action_task"
                android:orderInCategory="11"
                android:title="@string/action_newTask"
                app:showAsAction="never"/>

            <item
                android:id="@+id/menu.resetDB"
                android:icon="@drawable/ic_action_reset"
                android:orderInCategory="100"
                android:title="@string/action_resetDB"
                app:showAsAction="never"/>
            <item
                android:id="@+id/menu.listDB"
                android:icon="@drawable/ic_action_storage"
                android:orderInCategory="100"
                android:title="@string/action_listDB"
                app:showAsAction="never"/>
        </menu>
    </item>

</menu>